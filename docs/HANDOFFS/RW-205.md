## Completed

- Connected the frontend transaction page to the full live transaction lifecycle.
- Added edit, save, soft-delete and restore UI actions in addition to quick add and list.
- Extended the frontend API client and GitHub Pages demo adapter with update, delete and restore transaction calls.
- Added React Testing Library coverage for create -> edit -> delete -> restore.

## Files changed

- `frontend/randwise-web/src/api/client.ts`
- `frontend/randwise-web/src/features/transactions/TransactionsPage.tsx`
- `frontend/randwise-web/src/App.test.tsx`

## API or schema decisions

- No API contract changes.
- Frontend uses the existing canonical `/api/v1/transactions/{id}` update/delete and `/api/v1/transactions/{id}/restore` endpoints.
- Pages demo mode now soft-deletes locally so restore behaves like the real API.

## Tests run and results

- `npm run lint` passed.
- `npm run typecheck` passed.
- `npm run test` passed with 7 tests.

## Known limitations

- The UI still uses cents input for the first vertical slice; later budget/category UX work should add user-friendly rand input formatting.
- Category selection is not exposed yet; edits preserve the existing transaction category.

## Risks

- Live browser verification should be repeated when an E2E test harness is added.

## Required follow-up

- Wave 3 category management and budget work should replace the preserved-category edit path with explicit category controls.
