## Completed

- Added transaction list and quick-add frontend screen.
- Connected quick-add to the live `POST /api/v1/transactions` contract.
- Connected transaction list to the live `GET /api/v1/transactions` contract.
- Reused shared shell, panel, button and privacy-aware money display primitives.

## Files changed

- `frontend/randwise-web/src/api/client.ts`
- `frontend/randwise-web/src/features/transactions/TransactionsPage.tsx`
- `frontend/randwise-web/src/router.tsx`
- `frontend/randwise-web/src/App.test.tsx`

## API or schema decisions

- Quick add sends web-sourced expense transactions with integer cents.
- `categoryId` is sent as `null` so the first vertical slice uses the backend default category fallback.

## Tests run and results

- `npm run lint` passed.
- `npm run typecheck` passed.
- `npm run test` passed with 4 tests.
- `npm run build` passed.

## Known limitations

- Edit, restore and category correction UI are not implemented yet.
- Natural-language quick-add parsing is deferred; the structured quick-add posts directly to the canonical transaction API.

## Risks

- The frontend does not yet invalidate dashboard data after mutations because dashboard is still fixture-backed.

## Required follow-up

- RW-205 should verify the live register -> profile -> add transaction flow against a running API.
- RW-205/RW-207 should add end-to-end coverage when Playwright is introduced.
