## Completed
Expanded the dashboard frontend structure with live-ready sections for forecast, commitments, recent transactions and insights.

## Files changed
- `frontend/randwise-web/src/features/dashboard/DashboardPage.tsx`
- `frontend/randwise-web/src/shell/AppShell.tsx`

## API or schema decisions
The dashboard page uses the backend composite API shape and retains fixture fallback.

## Tests run and results
`npm run lint`, `npm run typecheck`, `npm run test`, `npm run build` and `VITE_DEMO_MODE=true npm run build` passed.

## Known limitations
Visual charting is still lightweight HTML/CSS instead of a charting package.

## Risks
The dashboard is more complete, but the detailed calculation breakdown remains pending.

## Required follow-up
Finish RW-407 and RW-408 before dashboard acceptance.
