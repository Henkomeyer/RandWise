## Completed

- Created a lightweight ADR directory and process notes for future architecture decisions.
- Added an initial accepted ADR that records existing MVP architecture constraints from `AGENTS.md` and the canonical specs.
- Created a handoff directory README describing the required handoff structure.
- Confirmed this task did not change canonical API, schema, UX, task graph, or implementation status documents.

## Files changed

- `docs/adr/README.md`
- `docs/adr/0001-mvp-architecture-constraints.md`
- `docs/HANDOFFS/README.md`
- `docs/HANDOFFS/RW-003.md`

## API or schema decisions

None. This task records existing MVP constraints only and does not alter API endpoints, DTOs, database tables, relationships, migrations, or schema rules.

## Tests run and results

- Read `AGENTS.md` and all existing files in `docs/` before editing.
- Manually checked new relative links and referenced paths.
- No code tests were run because this task only adds documentation.

## Known limitations

- ADRs are currently markdown-only and not enforced by tooling.
- The initial ADR is intentionally high-level; future implementation decisions should add focused ADRs rather than expanding ADR 0001 indefinitely.

## Risks

- Future agents may treat ADRs as canonical contracts. The ADR README clarifies that canonical API and schema contracts remain in the existing contract/spec documents.
- Because this workspace is not currently a Git repository, git-based verification of changed files was unavailable.

## Required follow-up

- Future agents should add ADRs for material architecture decisions, especially changes involving API shape, schema, authentication, persistence, AI provider behavior, background processing, or deployment architecture.
- Integration Agent should include these handoff and ADR docs in the Wave 0 baseline review.
