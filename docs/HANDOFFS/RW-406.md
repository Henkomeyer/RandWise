## Completed
Connected the dashboard frontend to `GET /api/v1/dashboard` through the shared API client.

## Files changed
- `frontend/randwise-web/src/api/client.ts`
- `frontend/randwise-web/src/features/dashboard/DashboardPage.tsx`
- `frontend/randwise-web/src/App.test.tsx`

## API or schema decisions
Demo mode now returns a contract-shaped dashboard response from local fixture data.

## Tests run and results
Frontend gates passed. Browser verification passed at wide desktop and mobile dark mode with no horizontal overflow.

## Known limitations
When no authenticated token is present, the dashboard intentionally falls back to the static fixture for tests.

## Risks
More dashboard-specific React tests should be added in RW-408.

## Required follow-up
Add privacy-mode dashboard breakdown tests and accessible text summaries.
