## Completed
Implemented safe-to-spend application service and an authenticated read endpoint.

## Files changed
- `src/RandWise.Application/Budgeting/ISafeToSpendService.cs`
- `src/RandWise.Contracts/Dashboard/SafeToSpendResponse.cs`
- `src/RandWise.Infrastructure/Budgeting/EfSafeToSpendService.cs`
- `src/RandWise.Api/Endpoints/DashboardEndpoints.cs`
- `docs/API_CONTRACT.md`

## API or schema decisions
Added `GET /api/v1/dashboard/safe-to-spend` as an intentional contract addition for budget-page consumption before the full dashboard aggregate is implemented.

## Tests run and results
`dotnet test RandWise.sln` passed, including safe-to-spend calculation coverage.

## Known limitations
When no category budgets exist, the service caps safe-to-spend by available cash after protected commitments.

## Risks
The formula is a first MVP calculation and should be reviewed against financial correctness requirements before beta.

## Required follow-up
Wave 4 should integrate this into the full dashboard response and add trend context.
